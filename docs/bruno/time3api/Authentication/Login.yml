info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/auth/login
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "marko.maric@interhome.group",
        "password": "bedranbedranbedran"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
